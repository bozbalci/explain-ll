fun isEven(x)
    if x == 0 return 1; endi;
    if x == 1 return 0; endi;

    if isOdd(x - 1) == 1
        return 1;
    else
        return 0;
    endi;
endf;

fun isOdd(x)
    if x == 0 return 0; endi;
    if x == 1 return 1; endi;

    if isEven(x - 1) == 1
        return 1;
    else
        return 0;
    endi;
endf;

result := isEven(42);
output result;
return 0;
